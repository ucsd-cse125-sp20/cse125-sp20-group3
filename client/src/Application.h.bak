#ifndef _Application_H_
#define _Application_H_

#include "../The-Forge/Common_3/OS/Interfaces/IApp.h"

#include "GLTFObject.h"

#define IMAGE_COUNT 3

class Application : public IApp
{
public:
    static uint32_t gImageCount, gFrameIndex;
    static mat4 projMat, viewMat;

    bool Init();
	bool InitializeShaderPrograms();
    bool CleanShaderResources();
	bool InitializeObjects();
    bool CleanObjectResources();
    void setRenderTarget(Cmd* cmd, uint32_t count, RenderTarget** pDestinationRenderTargets, RenderTarget* pDepthStencilTarget, LoadActionsDesc* loadActions);
    void Exit();

	void LoadPipelines();
    bool Load();
    void Unload();
    void Update(float deltaTime);
    void Draw();

    const char* GetName() { return "Application"; }

    bool addSwapChain();
    bool addRenderTargets();
    bool addDepthBuffer();
};

#endif